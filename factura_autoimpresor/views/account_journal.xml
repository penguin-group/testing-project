<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

        <!-- account.journal inherit form view -->
        <record id="view_account_journal_form" model="ir.ui.view">
            <field name="name">view_account_journal_form</field>
            <field name="model">account.journal</field>
            <field name="inherit_id" ref="account.view_account_journal_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='advanced_settings']" position="inside">
                    <group string="Impresión de factura autoimpresor" 
                        attrs="{'invisible':[('type','!=','sale')]}">
                        <field name="mostrar_qr" />
                        <field name="pie_factura" widget="html" options="{'codeview': true}"/>
                    </group>

                </xpath>

            </field>
        </record>

    </data>


</odoo>
