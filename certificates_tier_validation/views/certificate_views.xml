<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_certificate_filter_inherit" model="ir.ui.view">
        <field name="name">view.certificate.filter.inherit</field>
        <field name="model">certificate</field>
        <field name="inherit_id" ref="completion_certificates.view_certificate_filter" />
        <field name="arch" type="xml">
            <filter name="confirmed" position="after">
                <separator />
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['done', 'cancel','to approve'])]"
                    help="My Certificates to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="Certificates validated and ready to be confirmed"
                />
            </filter>
        </field>
    </record>

    <record id="view_certificate_list_inherit" model="ir.ui.view">
        <field name="name">view.certificate.list.inherit</field>
        <field name="model">certificate</field>
        <field name="inherit_id" ref="completion_certificates.view_certificate_list" />
        <field name="arch" type="xml">
            <list position="inside">
                <field
                    name="validation_status"
                    widget="badge"
                    optional="hide"
                    decoration-info="validation_status == 'pending'"
                    decoration-danger="validation_status == 'rejected'"
                    decoration-success="validation_status == 'validated'"
                />
            </list>
        </field>
    </record>
</odoo>
