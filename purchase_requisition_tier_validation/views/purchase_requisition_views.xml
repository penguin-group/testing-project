<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_purchase_requisition_filter_inherit" model="ir.ui.view">
        <field name="name">view.purchase.requisition.filter.inherit</field>
        <field name="model">purchase.requisition</field>
        <field name="inherit_id" ref="purchase_requisition.view_purchase_requisition_filter" />
        <field name="arch" type="xml">
            <filter name="inactive" position="after">
                <separator />
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['done', 'cancel','to approve'])]"
                    help="My Purchase Agreements to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="Purchase Agreenments validated and ready to be confirmed"
                />
            </filter>
        </field>
    </record>

    <record id="view_purchase_requisition_tree_inherit" model="ir.ui.view">
        <field name="name">view.purchase.requisition.tree.inherit</field>
        <field name="model">purchase.requisition</field>
        <field name="inherit_id" ref="purchase_requisition.view_purchase_requisition_tree" />
        <field name="arch" type="xml">
            <list position="inside">
                <field
                    name="validation_status"
                    widget="badge"
                    optional="hide"
                    decoration-info="validation_status == 'pending'"
                    decoration-danger="validation_status == 'rejected'"
                    decoration-success="validation_status == 'validated'"
                />
            </list>
        </field>
    </record>
</odoo>
