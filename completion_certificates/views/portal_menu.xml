<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Certificates to Portal Menu -->
    <template id="portal_my_home_menu_certificates" name="Portal layout : Certificates menu entries" 
              inherit_id="portal.portal_breadcrumbs" priority="40">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'certificate' or page_name == 'certificate_detail' or page_name == 'certificate_new'" 
                class="breadcrumb-item">
                <a href="/my/certificates">Certificates</a>
            </li>
            <li t-if="page_name == 'certificate_detail'" class="breadcrumb-item active">
                Certificate Detail
            </li>
            <li t-if="page_name == 'certificate_new'" class="breadcrumb-item active">
                New Certificate
            </li>
        </xpath>
    </template>

    <!-- Add Certificates to Portal Home -->
    <template id="portal_my_home_certificates" name="Portal My Home : Certificates entries" customize_show="True"
              inherit_id="portal.portal_my_home" priority="50">
        <xpath expr="//div[contains(@class, 'o_portal_docs')]/div[@id='portal_vendor_category']" position="inside">
            <div class="o_portal_index_card col-md-6 order-2">
                <a href="/my/certificates" title="Certificates" class="d-flex gap-2 gap-md-3 py-3 pe-2 px-md-3 h-100 rounded text-decoration-none bg-100">
                    <div class="o_portal_icon d-block align-self-start">
                        <img t-attf-src="#{'/completion_certificates/static/src/img/certificates.svg'}"/>
                    </div>
                    <div>
                        <div class="mt-0 mb-1 fs-5 fw-normal lh-1 d-flex gap-2">
                            <span class="fw-bold d-none" data-placeholder_count="certificate_count"></span>
                            <span>Certificates</span>
                        </div>
                        <div class="opacity-75">
                            View and create certificates
                        </div>
                    </div>
                </a>
            </div>
        </xpath>
    </template>
    
</odoo>